# Task D — Method and Experimental Specification

- Version: **TD-v1.2**, 2026-09-26. Owner: Minh. Class: METHOD_SPECIFICATION / FORMATION.
- Status: **REVIEW_READY WITH BLOCKERS — do not start E**. Technical design is **CANDIDATE**; human approval OPEN. Task E **NOT STARTED / NOT AUTHORIZED**.
- This is a development-selection/execution specification. No empirical selection, sensitivity, final configuration freeze, training or benchmark has been performed.

## 0. Authority, revision and sources

[U26 mission](../evidence/project-direction/2026-09-26-rca-task-d-redesign.md), [RCA-022–042](RESEARCH-DECISIONS.md), [DT18](../evidence/project-direction/2026-09-18-de-tai-va-nhiem-vu.md) and [advisor source](../evidence/advisor-direction/2026-09-23-huong-dan-do-minh-cung-cap.md) govern scope. C lock owns the RQ; B CLOSED §12 owns dataset facts; D owns candidate methods. Agent-selected formulas are not USER_CONFIRMED.

Prechange P: codex/rca-research-program at fa27a9d32873d957818ac389b8d3fc8f4e98b185. W: main at 38a0d0362e1e51a56ba3a6334a7f7c13a036f603. Older P main is not the current D source. TD-v1.1 SHA256 9a70e64db570916b9f02fcc81fab3f4bf7cef0313f5c2de213a9eb0a8d690f3c remains recoverable in Git and old receipts. Preserve the unrelated dirty README.

[Review packet](D:/Project/flash-ticket-rca-research/task-d/td-v1.2-review-packet.md), [memos](D:/Project/flash-ticket-rca-research/task-d/td-v1.2-review-memos.md), [provenance/data matrix](D:/Project/flash-ticket-rca-research/task-d/td-v1.2-provenance-and-data.md) and [validation](D:/Project/flash-ticket-rca-research/task-d/td-v1.2-validation.json) are detailed evidence, not competing decision sources. Native tools admitted **three distinct agents**, producing five roles and all five ring edges. **Five independent subagents remain unmet**, an assurance blocker before E.

Revision: scoped development-label permission; uncapped local evidence and available-modality fusion registry; PPR primary and value-diffusion secondary; conditional forecasting C5 plus graph-TV; executable contextual RCD; grouped selection, sensitivity and exact public roles. Preserve GT limits, historical exposure, planned denominators, immutable ranks, two project doors and DT18. Previously disclosed limitations are not newly discovered factual errors.

## 1. Research architecture and estimands

**C1 primary:** how much do observed service relations improve known-window root-service ranking with common telemetry, local evidence, candidates, supervision rights and budgets? ΔL=MRR(O)−MRR(L); ΔR=MRR(O)−mean_j MRR(R_j). This controlled contrast is not the entire graph method.

**C5 first-class graph anomaly detection:** graph restricts predictors of telemetry; forecasting residuals produce scores before decisions. Compare G with local L and all-other context ALL. Graph-TV is a distinct spatial secondary mechanism. C5 does not replace C1 and has separate GT/endpoints.

```text
Known-window controller → common M+T → C1 L/O/R → immutable ranking packet
Archival telemetry → prefix graph + fit/calibration → C5 G/L/ALL → trigger
  → common past-only integrated MTL diagnosis → packet → LLM explanation
```

Detector-selected cases never replace the planned C1 sample. C3 operation support and C4 graph placement remain; C2 optional after valid/stable C1. Triggered composition is not the primary estimand. LLM cannot repair ranks or create GT.

### 1.1 C3, C4 and C2 disposition

**C3: IMPLEMENT supporting operation evidence** (§4/12); **DEFER scored operation-aware representation/ablation** in the core C1 campaign. Adding operation-specific max features changes feature count, sparsity and service-operation multiplicity, introducing another contrast beyond the controlled relation effect. Packet counts/top changes support inspection without changing ranks; they do not establish representation utility. A separately reviewed extension after valid C1 must specify equal-dimensional/count-controlled pooling, matched telemetry/compute and service-only outcomes. No operation-root accuracy on RE2-TT.

**C4: IMPLEMENT graph participation after common local scoring, at late service ranking** (§5); **DEFER cross-placement comparison** with contextual/neural scorers. Changing scorer/capacity/features does not isolate the stage where graph participates. L/O/R tests this declared ranking mechanism, not all graph placements. C5 forecasting uses graph before detection in a separate task; its controls are not a C4 placement study, and detector F1 cannot be compared with known-window MRR to select placement. Reopening requires a separately reviewed matched experiment before extension outcomes.

**C2 remains optional:** mandatory window/bin sensitivity in §8 diagnoses this fixed-task design, not a separate evidence-budget/early-stop study. Neither C5's past-only windows nor sensitivity results automatically open C2; a new scope is required after valid/stable C1. These dispositions preserve mandatory topics without claiming unperformed ablations.

## 2. Data rights, supervision and exposure

Trusted controller retains paths, labels, τ and case metadata. Workers receive numeric arrays, masks, case-local indices, allowed adjacency and config only. No root_cause.txt, old predictions, metadata or label-bearing paths are mounted. An opaque handle is issued once for routing/seeds, never a feature or LLM input. Literal service names join identities, never embeddings or root priors. Evaluator opens final labels after predictions are sealed.

| Phase | Allowed | Forbidden |
|---|---|---|
| Development root/fault | Registered grouped variant/hyperparameter/model/sensitivity selection | Runtime feature; claiming entirely label-free development |
| Development τ | CANDIDATE supervised regime calibration/forecast objective under RCA-028 | Runtime prefix selection by τ; pretending Minh confirmed a particular injection-time calibration |
| Final labels/τ | Evaluation after seal; C1 τ external known-window slicing | Tuning/filtering/model choice/final threshold fitting; detector access to τ |
| Telemetry | Allowed window, prefix and past for the mode | Future suffix, full-case vocabulary/graph, metadata normal/fault counts |
| Logs | Redacted evidence and declared counts | Instructions, imagined span-event IDs, parser fitting using final labels |

[Historical exposure ledger](D:/Project/flash-ticket-rca-research/task-d/task-d-exposure-ledger.md): E1 already saw TT90 baseline outcomes and post-hoc fusion/strata. Claim **prospectively frozen new contrasts on a previously studied benchmark**, never clean/untouched confirmatory evaluation. RE3 exposure includes metadata, 90 footers and one raw case, not new method outcomes. New partitions cannot erase exposure.

Required canaries: remap labels/paths/τ/metadata counts with telemetry and realized random artifacts fixed, obtaining identical numeric outputs (except deliberately altered C1 controller slicing). Jointly permute indices, arrays and realized graphs, preserving mapped results/ties. Test future-suffix numeric invariance and C5 time translation. Full-file rehash can change retrospective sampled excerpts; no whole-packet/LLM byte-invariance promise.

## 3. Development, selection and freeze

Keep RE2-TT30 development/60 final, all three repeats of a cell together. Development: auth(cpu,delay), order(disk,loss), route(mem,socket), train(cpu,delay), travel(disk,loss). Final20 cells. Near-balanced, not balanced in every dimension; mem/socket less represented. Not held-out services/faults/system. Keeping the pre-outcome partition avoids reselection by difficulty; ten effective development cells remain a small selection sample.

Grouped folds, each two cells/six incidents, train24:

| Fold | Held-out cells |
|---|---|
| F1 | auth/cpu; order/disk |
| F2 | auth/delay; travel/loss |
| F3 | train/cpu; route/mem |
| F4 | train/delay; travel/disk |
| F5 | order/loss; route/socket |

F3 training lacks mem, F5 lacks socket. OOF is a selection criterion, not an unbiased post-selection estimate; no fold-SE significance.

Selection cascade before final outcomes:
1. Validate meaning/mapping/fixtures; register all sources/configs before development outcome jobs.
2. Select eight local variants (§4) using equal-cell OOF **absolute L-MRR**, missing roots/failures0. Never choose weak L to maximize apparent graph gain. Freeze common local θ for L/O/R.
3. Select six PPR configs using **absolute O-MRR**, not O−L/O−R. PPR remains primary even if secondary diffusion has a higher development score. Select secondary α among three values by absolute O-MRR; report all registered curves.
4. Select common C5 λ by equal-weight G/L/ALL normalized prediction MAE on held-out-development **pre-injection bins**, before residual rescaling. Target IDs are fixed from INPUT/PREFIX eligibility (finite target/own lag, fit/calibration row gates), before predictions and identically across three arms/three λ; equal eligible scenario→case→bin→channel weights. Input-insufficient cases do not supply loss observations but remain planned coverage denominators; require≥80%development cases with eligible IDs, otherwise return D. Objective is conditional on those fixed input IDs, not all cases. Any numeric prediction failure invalidates that configuration pending implementation correction/review; never shrink the scoring mask by finite predictions or use a lower missing penalty for unbounded errors. This is a study-specific supervised regime objective.
5. With λ fixed, each detector chooses q from three values using fold-training normal score distributions and held-out macro regime-F1. Same search budget. Refit thresholds on full development normal-score IDs only.

Ties≤1e−12: registered simpler choice, then lexical ID. Priority: max pooling before Q90; max fusion before availablemean; floor.01 before .001; reverse before undirected; damping.85 before .5 before .2; λ1 before10 before.1. These are conventions, not robustness theorems. All objectives undefined→return D. Local selection through L conditions the estimand on a graph-free-useful representation; it may miss representations useful only relationally and is not the uniquely fair policy.

Mandatory §8 sensitivity and leave-one-development-cell reselection winner counts/margins precede final freeze. D locks a registry/process today. Future authorized E/F conducts empirical selection. Before G require selection manifest, sensitivity report and configuration review; synthetic/string checks cannot replace empirical receipts.

## 4. C1 inputs and local evidence

Reference [τ−300,τ), query [τ,τ+300), bins10s/30 per side; controller removes τ/epochs after slicing. Select spans by startTimeMillis/1000, half-open. Duration/status semantics are not certified and excluded from primary; archival start time is not arrival time.

V=exact nonempty trace.serviceName in reference UNION query. Graph uses reference only; query-only nodes isolated. No metric/resource union or GT append. Trace parse failure is shared-input failure0; root absent0 evaluator-only.

Metric longest exact suffix allowlist: latency-90, latency-50, workload, diskio, latency, socket, error, load, cpu, mem. Prefix must exactly equal serviceName. Unknown/unmatched keys produce coverage counts, no aliases. Median bin requires≥5 distinct seconds; identical duplicates collapse, conflicting entity-second rows invalidate that channel within selected windows. No interpolation. Trace feature log1p(distinct traceID/spanID count); conflicting same-key payload is shared failure, identical duplicates collapse. Successful observed zero is different from missing input.

Valid channel:≥24/30 finite reference AND query bins. Trace count additionally needs≥1 reference span; do not create a huge anomaly from imputed reference zero for query-only nodes. Type7 scalar quantiles:

```text
c=median(reference); s=max(IQR(reference),εrel*median(abs(reference)),1e−12)
r_b=abs(query_b−c)/s; channel_score=Q90(r_b)
```

No cap or divide20. Overflow/NaN is numerical channel failure, never silent clipping. Eight variants: εrel{.001,.01} × metric-channel pooling{max,Q90} × fusion{max,availablemean}. T has one count channel. Availablemean averages AVAILABLE M/T blocks, avoiding fixed/2 missing discount. Both absent→local0+mask, not healthy. Arrays/masks/selected local vector identical L/O/R. Publish channel multiplicity, constants/floors/extreme tails, absent-reference/query-only/all-tie fractions; paired controls do not remove local representation bias.

Robust deviation principle **ADAPTED_FROM_PRIOR_WORK** [BARO](https://arxiv.org/html/2405.09330v1); this whole pooling/fusion/window scheme is not BARO. Old cap20/fixed fusion are development diagnostics. No post-evaluation semantic upgrade.

Logs excluded from primary C1. Exact container_name==serviceName; timestamp seconds. Log-metric exact entity-second is bin association; log-trace service+time only. LOGCOUNT counts rows, not deduplicated messages without event IDs. Invalid/malformed coverage retained; missing file is not zero-success. Unknown entities not repaired. C3 literal service/operation counts/top3 absolute rate changes are evidence, not operation-root predictions. Redacted query logs≤3/service chosen by smallest SHA256(source_file_sha|original_row_ordinal), never root/severity. E/F must audit every used schema/join.

## 5. Ranking and structural controls

Observed u→v requires same-trace resolved parent and BOTH spans inside reference, different services. Binary relation dedup/count provenance; null/unresolved parents never guessed. Resolution denominator nonnull selected-reference parents; unresolved boundary-censored evidence is not topology recall. No outside-window repair.

**PPR-adapted-TD12 primary:** binary reverse-call projection (v→u from observed u→v), or declared undirected development variant. Mechanism lineage [MicroRCA official](https://github.com/elastisys/MicroRCA/blob/master/MicroRCA.py); not its correlation/host-weighted full method or causal direction.

```text
T row-stochastic; isolated row self-loop; m=max(l); p=(l/m)/sum(l/m)
π=(1−d)p+d*Tᵀπ; d∈{.2,.5,.85}
```

sum(l)=0→common all-tie/no-evidence, never silent uniform PageRank. Six configs: two directions×three damping values; .85 prior implementation reference, .2/.5 study sensitivity. L T=I, O observed, R perturbed; common p/d/budgets. Direct float64 solve, residual∞≤1e−10, minπ≥−1e−12, |sumπ−1|≤1e−10; clip tolerated tiny negative roundoff only, renormalize/check. Other numerical failure0. Rank decreasing round(score,12), retain ties; no legacy100-step guarantee.

Uniform-positive personalization can induce topology ranking: **constant-output invariant does not apply to PPR**. Report label-free uniform-personalization, degree/reachability diagnostics. R preserves degrees, not spectrum/paths/kernel/centrality. O−L includes mixing/topology priors; O−R arrangement relative to finite perturbations, not every nuisance or causal information.

**VALUE-DIFFUSION-TD12 secondary:** undirected row-stochastic P, q=(1−α)l+αPq, α{.2,.5,.85}. For numerical stability solve y=(1−α)(l/m)+αPy with m=max(l)>0; rank normalized y rounded12, save raw scale m as provenance. If m=0 common all-tie. This avoids absolute-residual failure with uncapped large evidence; direct float64 solve checks residual∞≤1e−10 on the bounded normalized equation, finite values and min/max convex bounds with1e−10 tolerance. Value smoothing is not PPR mass. Constant-vector/max-bound invariants apply only here. Same θ/V/features/rights; separate undirected R when primary is reverse. Report secondary regardless of outcome, never switch/rescue primary. Both ranking operators are one walk/resolvent family, not all graph methods.

### 5.1 R finite perturbation contract

256 independent PCG64 chains/case/representation; seed first64bits SHA256('TD12|R|fixed_opaque_handle|representation|j'),j0..255. Fixed issued handles and realized graphs in canaries, no service names. Each starts O; exactly200*max(1,edgecount) proposals, rejected self-transition, no outcome/adaptive stop.

Directed: uniformly sample two distinct edges(a,b),(c,d), require four distinct endpoints, propose(a,d),(c,b). Reject loops/duplicates/existing new edges and changes to exact weak-component vertex partition. Preserve per-node in/out degree. Undirected: orient sampled edges independently by .5 coins, same switches/rejections, preserve per-node degree and connected partition. Isolates/unswitchable graphs unchanged+degeneracy; no fabricated graph. Save every seed/adjacency hash/acceptance/overlap/changed-node fraction.

Development topology-only proposal-budget100/200/400E sensitivity, no root outcomes/O−R. Primary200 unless reviewed topology/resource amendment before G. Study mobility gates:≥32 distinct finals and median retained-edge fraction≤.8 per case;≥80% planned final cases and≥50% each evaluator root stratum before strong arrangement interpretation. Not mixing proof; never remove failing cases. Final validation-only metadata/graph job produces no predictions.

Mean metrics over ALL256 draws, failures0; never metric of mean scores, no seedN. Report measured MCSD/SE per case and aggregate. Conditional bounded SE≤.03125/case; mean60cases≤.00403 (approx95%halfwidth.0079), assuming independent draws, not uniformity/mixing/incident independence. No permutation p-value. Any R failure/insufficient mobility limits informative arrangement verdict; operational utility still reported.

## 6. Mandatory contextual baselines and fidelity

**Local-MAX-MT** (max selected M/T blocks), **BARO-RANK-adapted-TD12**, **RCD-RCAEval-adapted-TD12** are mandatory executable comparators. No installation/reproduction today. L/R answer internal attribution; external methods compare different complete methods.

BARO adapter: max query absolute deviation/reference robust scale, common10s metric bins/V, service max, selected floor, missing0+mask/ties12. No MBOCPD detector. Downshift/spike/constant fixtures and paper/upstream fidelity delta required. Modality mismatch: O−BARO not pure relation gain.

RCD [RCAEval source](https://github.com/phamquiluan/RCAEval/blob/7600283af1ea5e2e9fff6f07124951d0e989de42/RCAEval/e2e/rcd.py); original RCD373882c6982db7a999ec1ff99ea54c644a48b409. Wrapper bins5(kmeans),gamma5,localizedTrue,dk_select_useful=False,dataset=None; inherited LOCAL_ALPHA.01/finalstart.001,step.1,limit1. Seeds420/421/422, no best seed. Original binsNone is not equivalent. E verifies license, causal-learn compatibility, env and patches.

Raw1s exact-mapped metrics, same300/300 windows. Eligible channel≥240/300 finite distinct seconds EACH side, reference IQR>0; conflicting duplicate invalidates channel. Impute remaining missing seconds reference median only, no query median/full-corpus fit. **Patch pinned wrapper immediately AFTER its known-window split to drop time from normal_df/anomalous_df before run_multi_phase/CI**; do not remove it before the wrapper's required slicing or leave it as a feature. Save patch SHA and fixture proving no time column reaches CI or ranking. No useful-column/domain/label filtering. No eligible columns→failure0. Kmeans on both selected windows is declared unlabeled known-window preprocessing.

Metric ranking→exact owning service FIRST OCCURRENCE; unknown keys discarded+coverage; ranked services preserve source order; all remaining V ONE WORST TIE. Partial padding allowed only for this adapter. Unparseable/duplicate/NaN output failure; average all three per-seed metrics, failed seed0. Learned metric graph is not trace graph/causal GT; raw1sM versus common10sM+T makes this contextual, not exact-paper/SOTA reproduction.

MicroRank duration/status/incomplete-trace audit unresolved. CIRCA needs justified conditional structure, not renamed trace edges. Eadro/DéjàVu no longer categorically excluded by superseded dev policy, but full root-head fitting/target units/data budgets are unapproved/unmatched here, literature-only. GDN learned graph/attention and upstream test-error normalization/report-best differ from this protocol. No GNN mandate. If RCD infeasible, retain errors and return D for a reviewed compatible comparator BEFORE final outcomes; never silently downgrade.

## 7. C5 conditional forecasting

**G-RIDGE-FORECAST-TD12** adapts graph prediction/residual ideas from [GDN](https://arxiv.org/abs/2106.06947) and conditional residual reasoning from [CIRCA](https://arxiv.org/abs/2206.05871). Not reproduced GDN, causal CIRCA or new algorithm. Ridge is standard; observed graph/pooling/prefix are study-specific. Small archives/limited training favor a transparent bounded fit; no superiority theorem.

Runtime s0=first observed metric timestamp;5s bins,180s warmup/36bins. First24/120s fit graph/scalers/models; final12/60s HELD-OUT residual scaling. V from warmup traces, absent-fit channels MODEL_ABSENT, no future union. Static prefix graph afterward. Metadata injection offset720s RE2TT/900s RE3TT places prefix before DECLARED injection; independent healthy state/arrival times not certified. Worker never reads τ/normal counts; after-seal ambient/prefix diagnostics, no GT reset/exclusion.

Metric5s median≥3 distinct seconds; exact suffix/maps; log1p(counts), successful zero+mask. Trace/log count targets require at least one row for that service in the **first24 fit bins**; otherwise MODEL_ABSENT. Once eligible, successfully loaded empty bins are observed zero; missing/corrupt files stay unavailable. Streaming conflicts invalidate encountered bin from cutoff, never future-global validation retracting past decisions. Normalize by **first24/120s fit-bin** median/IQR only, selected εrel/1e−12; held-out12 bins never fit these scalers. Same suffix/type neighbors are necessary, not sufficient semantic comparability.

```text
x=[own z(t−1),mean callee same-c z(t−1),mean caller same-c z(t−1),
   callee available fraction(t−1),caller available fraction(t−1)]
minimize mean(y−b−xβ)^2+λ||β||²; intercept unpenalized;λ{.1,1,10}
```

Missing neighbor pool→mean0+coverage0 sentinel, denominator frozen same-type applicable degree; no edge→0. Missing own lag/target→no fit/residual, never healthy-target imputation. ≥18 valid fit rows out of23lag1 possibilities;≥9 valid calibration residuals out of12. Fewer means model unavailable. Float64 stable solve/design-rank diagnostics.

Held-out e=abs(z−prediction); μe=median(e), se=max(IQR(e),εe), εe=.01 primary; r=max(0,(e−μe)/se). Constant/floor flags;9–12 correlated errors are noisy SCALE estimates, **not per-case q99 or probabilities**. System S=max valid r across channels/nodes; none→UNAVAILABLE. Save counts, active rank, effective ridge degrees of freedom, errors/scales/masks/model count. Max multiplicity remains a limitation.

Primary C5 arms G-MTL/L-MTL/ALL-MTL; registered modality-secondary G-MT/L-MT/ALL-MT. Same bundle/V/masks/prefix/tensor5slots/λ/fit budget. L forces neighbor slots/fractions0. ALL fills both means with all-other same-type available service mean and both fractions with available/all-applicable-other count. **Active capacity differs despite nominal dimension**; publish it. ALL tests generic context advantage, not perfect capacity matching. Missing logs do not veto M/T; no fixed/3. Trace structure and trace counts separate; no invented log edges.

### 7.1 Thresholds, replay and valid GT

For fold-training normal score IDs after warmup with bin end≤τ, fit **weighted empirical CDF inverse** Qq: smallest finite value whose cumulative weight≥q. With S_score the scenarios containing at least one scored normal case, weight(s,i,t)=1/|S_score| ×1/n_scored_cases(s) ×1/n_valid_normal_bins(i); normalize/check weights sum1. This is a conditional scored-support CDF, equal scored-scenario→scored-case→valid endpoint, not equality over missing planned cases. q{.95,.975,.99}; differs from type7 scalar-channel quantiles. Empty-normal cases remain planned coverage and F1 denominators. Require≥100 normal endpoints and≥80%training cases with any normal score, otherwise calibration invalid/return D. Never fit final tails.

Choose q by held-out **macro incident system-bin injection-regime F1**; failure/no scorable bins0+undefined flag. Tie higher q, then config ID. Refit chosen threshold only on full development normal-score IDs. Strict S>threshold; label-guided selection, not productionFPR calibration. Separate per-case prefix fit, per-case residual scale, fold threshold fit and held-out selection.

First score endpoint185s. Bin positive if start≥τ, negative if end≤τ; straddling excluded+count. Raw exceedance before persistence. Per-case P/R/F1 on valid/evaluable bins, undefined denominator0+flag; macro planned60 and pooled confusion descriptive. Publish planned/scored/unavailable/failed/excluded and both-regime coverage. Matched-bin subset only secondary; no bin-IID CI/nodeF1/onset truth.

Trigger three consecutive valid positive5s bins; unavailable/nonpositive resets streak; refractory300s since last trigger. First possible195s. Fixed event policy, not optimized via raw-bin F1. Report all triggers/pre-injection observed-hour replay rate/first post-injection delay, censored if absent; not operationalFPR/SLA.

At every trigger common integrated diagnosis uses PAST-ONLY reference[t−360,t−60),query[t−60,t),10s bins. Before360s history INSUFFICIENT_HISTORY, trigger retained. Same V/maps/selected PPR; reference≥24/30 and query≥5/6 valid bins. Each channel uses reference median/IQR floor and temporal **Q90 absolute normalized query deviation** as §4. Trace/log count requires≥1 reference row for that service; otherwise unavailable, not imputed normal-zero evidence. Once eligible, successful empty bins count zero; missing file unavailable. M/T/log blocks use selected metric-channel pooling and max/availablemean over available blocks. Separate cache/profile TD12-INTEGRATED-MTL; no R/comparator per trigger. FIRST post-injection trigger composed RR/Hit/NDCG; absent/insufficient-history/failure0. Later correct trigger cannot rescue first; composition does not replace C1.

### 7.2 Distinct secondary GRAPH-TV-TD12

[Gopalakrishnan/Li/Balakrishnan CDC2019](https://web.mit.edu/hamsa/www/pubs/GopalakrishnanLiBalakrishnan_CDC2019.pdf) spatial graph-signal mechanism **ADAPTED_FROM_PRIOR_WORK**, no Gaussian guarantee. Same prefix signed-standardized channels and undirected binary edges. TVc=MEAN over valid edges of (z_u,c−z_v,c)^2;≥1 comparable edge; S=maxcTVc. No edges→UNAVAILABLE spatial score. Shared development weighted-q threshold policy. Local magnitude max|z| separately reported, never silently mixed with TV.

Equal common-mode shifts can leave TV0; isolates blind; heterogeneous healthy services rough. Registered spatial detector, NOT C1 ranking robustness or automatic fallback winner. Report all outcomes; TV cannot rescue C1/forecast failure.

## 8. Mandatory sensitivity, registry and backups

Full provenance matrix in W. Every numeric gate/time/pooling/search/tie choice is **STUDY_SPECIFIC_DESIGN** unless otherwise classified. No fabricated citation.

| Dimension | Primary/registry | Mandatory DEVELOPMENT sensitivity |
|---|---|---|
| C1 bin/windows | 10s;300/300 | bins5/10/20 same horizon;windows180/300/420 at10s;valid gate ceil(.8N),seconds ceil(.5bin) |
| Evidence/floors | median/IQR;εrel.001/.01 | .0001/.001/.01;constant/extreme tails;oldcap20/fixed fusion;temporal max vsQ90;channel/mask curves |
| PPR | reverse/undirected;d.2/.5/.85 | all six OOF curves;uniform-personalization/degree/reachability |
| R | 256chains;200E | topology-only100/200/400E;MCSD/SE percase+aggregate;no outcome-adaptive budget |
| C5 bin/prefix | 5s/180s;24fit12cal | 10s/18bins:12fit6cal,minfit8/cal5,seconds5;240s/5s:32fit16cal,minfit24/cal12 |
| C5 predictor | lag1;λ.1/1/10 | G/L/ALL λ loss/F1 curves;lag3 own1–3+neighborlag1(7slots+intercept),≥20fitrows;εe.001/.01/.1 |
| Threshold/event | q.95/.975/.99;streak3;refractory300 | all q;streak1/3/5,refractory60/300/600 EVENT-only diagnostics |
| Effect/stats | δ.05;50kbootstrap/family2 | development precision/leave-cell/root/fault diagnostics;not outcome-tunedδ/CI |
| RCD | bins5,gamma5,three seeds | fidelity/missing/imputation fixtures;bins3/5/7 contextual dev sensitivity |

One factor at a time around selected config, no Cartesian grid. Registry8local+6PPR+3diffusion,λ3 thenq3/arm. Sensitivities are diagnostics, not secret search. Adopting a sensitivity variant requires reviewed finite-registry amendment BEFORE final freeze and logs of extra development attempts. Invalid mapping/coverage/starvation means return D. Registered-effect sign flips and leave-cell winner/margin instability trigger a documented review of robustness/claim limits, **not automatic retuning until gain is positive**. A valid unstable/negative method can freeze with those limits disclosed; no final winner selection.

Registered backups: row-diffusion ranking, graph-TV detector; ALL context diagnostic. Both rankers one family, TV different task. Duration/status/log-template extensions are **not current registry**: actual field meaning/units, prior lineage and bounded development proposal BEFORE extension outcomes. If frozen volume transfer fails, report bounded evidence weakness or separately version exploratory work, not post-result rescue.

## 9. Metrics, conditional uncertainty and verdicts

Primary tie-aware MRR planned60. Root tie positions a..b: RR=mean(1/r);Hit@k=max(0,min(b,k)−a+1)/(b−a+1). Root absent/method failure0; valid all-tie is not failure. Secondary Hit1/3/5,NDCG5 binary root relevance,IDCG1: sum_(r=a..min(b,5))1/log2(r+1)/(b−a+1), full tie denominator. Cost/coverage/ties separate. R averages per-draw metrics including failures, no seedN.

RE2 case mean equals equal-cell mean ONLY because each planned cell has three repeats. RE3 unequal repeats: **equal-cell mean of within-cell means headline**, case mean secondary. No pooled heterogeneous systems/tasks.

Finite Δ estimates first.50,000 paired scenario-block bootstrap of20cells/all3repeats/allarms,PCG64seed20260926,type7[.0125,.9875],97.5%two-sided EACH. Family exactly two; Bonferroni nominal simultaneous95% under exchangeability approximation. Shared campaign/crossed root-fault dependence remains: **not population confidence/significance proof**. Root leaveout5/fault leaveout6 point effects, scenario scatter/repeat ranges mandatory. All deletion effects>0 for stability; δ crossings reported as heterogeneity.

δ=.05 absoluteMRR studySESOI, not SLA/operator utility: six of60 rank2→1 yields.05 illustration. Development power/precision diagnostics are not measured80%power. For each contrast, optimistic normal planning with20 independent cells and known paired-cell SD uses Δplanning≈δ+(z_.9875+z_.8)*SD/sqrt(20), matching its97.5%two-sided interval lower bound. SD.1/.2/.3 gives effects roughly.119/.188/.257 for80%planning power of that individual contrast. This is not80%joint support power for both contrasts, measured corpus power or a remedy for campaign dependence.

Informative-data conventions retained from TD-v1.1, STUDY_SPECIFIC_DESIGN: signal starvation when≥80%planned final incidents have emptyV or identical round(local/max(local),12) values (allzero handled separately as all-tie); insufficient shared input when>10%planned final incidents have shared preprocessing failure. Either makes relation attribution INCONCLUSIVE. Any L/O/R arm-specific execution/numerical failure also makes attribution INCONCLUSIVE, while operational utility remains0 for failures. Target absence is a per-case diagnostic/zero outcome, no additional post-hoc aggregate exclusion or gate; any bounded negative must state candidate-visibility limitations and cannot infer graph utility where the target was unavailable. External baseline failure does not change C1 verdict. These gates are fixed before outcomes; local ceiling alone is not an inconclusive veto and may yield a bounded negative.

Order invalid→informative inputs/controls/execution→intervals/stability:
- BOUNDED SUPPORT: both conditional lower bounds>δ, all deletion effects>0, informative/no arm failures.
- BOUNDED NEGATIVE FOR FROZEN PRIMARY: valid informative study, upper of at least one required contrast<δ; conjunction unsupported, no equivalence/all-graph claim.
- INCONCLUSIVE: intervals intersectδ, instability/weak mobility/local starvation/wide uncertainty/arm failures. Operational point estimates still reported.
- INVALID: leakage/unequal inputs/GT repair/changed split-endpoint/winner-only/silent exclusion/wrong evaluator. Preserve run/errors.

Secondary/context/C5 estimation-only, never rescue primary by metric/seed/subgroup. Bootstrap/seeds do not create independent campaigns or clean exposure.

## 10. Failure attribution and resources

```text
No gain → implementation/evaluator/firewall invalid? INVALID, retain/fix/review.
 → identity/time/schema/masks wrong? mapping failure, not graph negative.
 → root absent/local evidence starved? coverage/signal limit, planned0.
 → observed graph/mobility weak? relation/control availability limit.
 → non-graph baseline near ceiling? remaining room, not graph useless.
 → representation/operator/parameters mismatch? registered dev sensitivity.
 → secondary agrees? walk-family only; TV detector is a separate task.
 → uncertainty too wide? INCONCLUSIVE; seeds do not create campaigns.
 → valid informative frozen primary upper<δ? bounded negative exact setup.
 → broader graph claim? more independent compatible evidence needed; absent.
```

C5 attribution: prefix fit/healthy-reference assumption→same-type neighbor coverage/active capacity→held-out scale→threshold availability→proxy-GT mismatch→ALL generic context→TV common-mode blindness. Telemetry-silent faults possible; root≠anomalous node.

Failures/missing roots0 planned denominator; low-resolution cases retained, unavailable not healthy. Required trace corruption shared0; method crash/timeout0. RCD partial padding sole exception to full-ranked-V rule. Common L/O/R timeout=max(300s,10*maximum successful development aggregate case walltime across arms); R includes all256, remaining draws0 after timeout. Diagnose failed dev runs before freeze; measured safe memory/host policy, stream/spill with equality fixtures, never delete expensive cases. Publish cold I/O/shared preprocessing/control/fit/prediction costs, not cachedL versus coldO.

## 11. Public task compatibility and roles

RCAEval revision afeacb11bcc94dadfd1c8f483ee4377b2b8b614e. [Metadata receipt](D:/Project/flash-ticket-rca-research/task-d/td-v1.2-public-metadata.json) is footer-only: **no new telemetry rows**. Detailed matrix separates metadata/full-trace/sample facts from unknown actual-use properties.

| Dataset | Facts/GT | CANDIDATE role | Admission/claim limit |
|---|---|---|---|
| RE2-TT90 | M90/T90/L89;5roots×6fault×3;serviceGT/fulltraceaudit | Primary C1+C5proxy+baselines | Actual window/prefix/all-used joins E/F;no node/operationGT |
| RE3-TT30 | All M/L/T;2roots8cells3–6repeats;codefaults;90footers/one rawsample | **All30 frozen RE2-selected transfer**,equal-cell estimation,no RE3outcome tuning | Same-system fault-family extension,not unseen service/campaign;29 raw joins/parent/windows unknown |
| RE3-OB30 | Tri-modal;4roots10cells×3 | Frozen cross-application codefault candidate | Exact maps/schema/license/window/prefix;not pooledTT |
| RE2-OB90 | Tri-modal5×6×3;two shortquery210/284s | Resource/network cross-application optional | No copy300query without failure policy/review |
| RE2-SS90/RE3-SS30 | M+L,noT;RE3answer files8 | LocalM/L/BARO/RCD portability | No observed-trace C1/G replication;learned metric graph distincttask |
| RE1-SS125 | Metric-only | Literature/component compatibility | No tri-modal/tracegraph |
| LEMMA ProductReview Preprocessed | df25484004e50483de6f6756c3a4d7ab03174b83;4M+4Lzips,no declaredT | Exact20211203 entity-ranking/longtime component candidate | PPTX GTcardinality/maps/rawschema OPEN;faulttimeJST→UTC−9h;license discrepancy |
| LEMMA Cloud Preprocessed | 03f82a2c4b16afe09e9315def9f5d6990427000a;6M+5Lzips;20240207CloudTrail-only | Component/justified learned-graph candidate | No assume allEKSlogs/no invented spans |

Local RE3 route/f2_1:27services,300s reference54relations,34996/34996 parents resolved/root visible. One sample, not30 certification. Fullsample log-metric74449/74449 does not prove log-trace:23exact trace-service matches/48containers. Footer uniform fields do not certify joins/availability/topology. LFS SHA is official object identity, not independently recomputed remote full-file hash.

RE3 role specified now. Frozen transfer means SAME RE2-selected localθ/operator/d/λ/q **and the RE2-fitted numeric detector thresholds**; no unlabeled RE3 threshold refit. Only already-declared per-case prefix graph/scaler/model/residual-scale fitting is runtime adaptation. Any alternative domain calibration requires a distinct preregistered adaptation arm reviewed before output opening. Authorized E/F audits every used file's units/clock/null/dup/identity/channels/window/prefix/reference parents/evaluator-only root/joins before campaign, no outcome-guided exclusion. If RE3 tuning necessary, STOP opening transfer outcomes, new D addendum with grouped dev/eval BEFORE outcomes. SS/LEMMA test honest different components; RE3OB distinct application candidate. Human accepts exact campaign scope. No full retrieval/training today; FlashTicket not another public dataset.

## 12. Packet, LLM and system boundary

TD-packet-v1.2:method/code/config hashes; immutable rank/score/tie interval; local evidence/ref-query/masks; raw observed parent direction/count/evidence IDs; **processing direction/operator separately**; C5 prefix/model/residual/threshold/trigger/profile; quality/maps/isolates/model-absent/numerics/uncertainty; redacted operation/log support. No τ/root/fault/answer/case path/root presence. Scores suspicion/residual utility, not root probability/causal truth; opaque IDs traceable in control manifest.

LLM outputs explanations(service_key,evidence_ids,text),uncertainties,suggested_checks only. Reject rank/score mutation/assertions; deterministic packet renderer. Test before/after digest, wrong-rank/missing-evidence/untrusted-log fixtures. I owns provider/model/prompt/rubric; faithfulness/unsupported claims/usefulness separate from root correctness. No LLM run/autofix/rerank.

Trusted research ObservationBundle adapter maps metrics/spans/logs/masks/hashes; C1 WindowRequest slices; numeric worker noGT/paths/epochs; C5 chronological prefix/lag policy. H verifies target clock/arrival/controlled GT and versions adaptations.

System references only here: [R0 §3](R0-boi-canh-va-rang-buoc.md), [system→research door](../project/lien-ket-rca.md), [roles](../project/roles.md). Independent-scope test PASS for document formation, not runtime integration. No API/schema/service/Saga/invariant changes. DT18 actual FlashTicket construction and throughput/latency/error/scaling/consistency/transaction-stability duties remain; read-only mechanism/explanation integrated at system gates. Minh/Sơn/Tuấn/Tuyến; mobile optional. Old demo-only advice does not reduce DT18.

## 13. Engineer handoff, freeze and OPEN

E entry requires human protocol acceptance plus explicit E scope; this mission supplies neither. Reproduce PPR/row/G-L-ALLridge/TV/LocalMAX/BARO/RCD contracts; validate all actual-used inputs; tune only registered development; perform sensitivity; retain every failure. No scientific choices improvised at G.

Future run manifest:TD/hash/source/configregistry;upstream SHA/license/patch;datasetrevision/filehash/bytes;opaqueplannedcases/split/exposure,separateGTmap;codecommit+dirtydiffhash;envlock/hardware/command/seeds;features/model/calibration-score IDs/graphhashes;everycase/draw prediction/error/cost;evaluatorversion/outputhash. Cache mode/cutoff/data/config identity, no C1/C5 contamination.

Freeze requires source/fidelity/env checks; boundary/missing/duplicate/mass/solver/tie/Rdegree-component/metadata-remap/prefix-future/LLMrank fixtures; actual-use mapping; empirical sensitivity/selection-instability report; threshold IDs/forecast-loss objective; resources/MCmobility; adjudicated review; selected configs sealed; workers no labels; updated exposure. Material change returns D before G. G one locked campaign, sealed predictions before labels, all primary/secondary/errors. Post-outcome changes exploratory/versioned, old runs preserved.

OPEN: five-distinct-reviewer assurance; Minh acceptance/E scope; E/F empirical compatibility/selection/sensitivity/fidelity/resources; extension license/authorized campaign; G effects; H target/system evaluation; I explanation; original advisor date/channel. No empirical benefit/runtime/cross-public completion/sensitivityPASS inferred from D math checks.
